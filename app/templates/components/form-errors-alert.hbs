{{#if (and showErrors (gt errors.length 0))}}
  <div class="alert alert-danger">
    <strong>{{fa-icon 'ban'}} Whoops there was a problem.</strong>
    <ul>
      {{#each errors as |error|}}
        <li>{{error}}</li>
      {{/each}}
    </ul>
  </div>
{{/if}}
