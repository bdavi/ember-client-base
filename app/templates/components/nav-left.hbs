<section class="sidebar">
  <ul class="sidebar-menu">
    {{nav-left-header title="MAIN NAVIGATION"}}

    {{nav-left-item
      title='Profile'
      icon='bank'
      linkPath='authenticated.organization'
    }}

    {{#nav-left-item
      title='Users'
      icon='users'
      hasChild=true
    }}
      {{nav-left-item-sub-item
        linkPath=(array 'authenticated.organization.memberships' organization.id)
        icon='user'
        title='Current Users'
      }}

      {{nav-left-item-sub-item
        linkPath=(array 'authenticated.organization.membership-invitations' organization.id)
        icon='envelope'
        title='Invitations'
      }}

      {{nav-left-item-sub-item
        linkPath=(array 'authenticated.organization.membership-invitations.new' organization.id)
        icon='plus-circle'
        title='Invite New'
      }}
    {{/nav-left-item}}
  </ul>
</section>
